# Realtime Stream Chat Application

A small app to test stream API

# Usage:

1.Clone repository:

```bash
git clone git@github.com:jame-hv/little-stream.git
```

2.Setting up .env (/backend/.env && /frontend/.env)

3. Run by docker

```bash
docker compose up -d --build
```
